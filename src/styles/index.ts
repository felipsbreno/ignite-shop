import { createStitches } from '@stitches/react';

export const {
  config,
  styled,
  css,
  keyframes,
  theme,
  getCssText,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6',
    },
  },
});
